type Query {
  info: String!
  feed: [Link!]!
  findLink(id: ID!): Link
}

type Mutation {
  post(url: String!, description: String!): Link!
  updateLink(id: ID!, description: String!): Link
}

type Link {
  id: ID!
  description: String!
  url: String!
}
